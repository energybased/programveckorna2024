using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrevorWorker : ArbetareStats
{
    // Start is called before the first frame update
    void Start()
    {
        workerSpeed = 2;
        workerService = 3;
        workerQuality = 1;
        restingTime = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
